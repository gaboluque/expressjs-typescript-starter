import { __modelName__(pascalCase)Repo } from '../__modelName__(camelCase).repo';

export const __modelName__(camelCase)Shower = (__modelName__Id: string) => {
  // @ts-ignore
  return __modelName__(pascalCase)Repo.findById(__modelName__Id);
};
