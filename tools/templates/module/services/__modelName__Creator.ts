import { I__modelName__(pascalCase)CreateDTO } from '../__modelName__(camelCase).types';
import { __modelName__(pascalCase)Repo } from '../__modelName__(camelCase).repo';

export const __modelName__(camelCase)Creator = (__modelName__DTO: I__modelName__(pascalCase)CreateDTO) => {
  return __modelName__Repo.create(__modelName__DTO);
}
