import { Document } from 'mongoose';

export interface I__modelName__(pascalCase) {
  // TODO: Define interface
}

export interface I__modelName__(pascalCase)Doc extends Omit<I__modelName__(pascalCase), '_id'>, Document {}

export interface I__modelName__(pascalCase)CreateDTO {
  // TODO: Define interface
}
export interface I__modelName__(pascalCase)UpdateDTO {
  // TODO: Define interface
}
