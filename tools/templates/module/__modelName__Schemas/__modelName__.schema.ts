import mongoose from 'mongoose';
import { LooseObject } from '../../../utils/commonTypes';
import { I__modelName__(pascalCase) } from "../__modelName__(camelCase).types";

const __modelName__(camelCase)Record<keyof I__modelName__(pascalCase), LooseObject> = {
  // TODO: Define schema
};

export const __modelName__(camelCase)sSchema = new mongoose.Schema(__modelName__(camelCase)SchemaFields, {
  timestamps: true,
  _id: true,
})
